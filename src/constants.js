/** Splash screen duration before the main site appears (ms). */
export const LOADER_DURATION_MS = 7000;

/** Work section: desktop layout uses hover previews from this width up. */
export const WORK_DESKTOP_MEDIA_QUERY = "(min-width: 768px)";
